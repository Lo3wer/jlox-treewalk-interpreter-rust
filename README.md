# jlox-treewalk-interpreter-rust
Rust implementation of the jlox interpreter. Interprets lox.

## Usage

### Run a file

```sh
cargo run -- path/to/script.lox
```

### REPL (interactive mode)

```sh
cargo run
```

This starts an interactive prompt where you can type Lox expressions and statements directly.
